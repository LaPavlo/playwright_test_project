import {test} from "../utils/fixtures";
//import {userToRegister} from "../utils/config";

test("Register new user", async ({ loginPage, header }) => {

   // await loginPage.register(userToRegister.newUser)
})